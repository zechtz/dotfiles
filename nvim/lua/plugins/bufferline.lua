return {
  "akinsho/bufferline.nvim",
  enabled = false,
  opts = {
    options = {
      -- LazyVim defaults to "tabs" (each entry = a vim tab).
      -- "buffers" mode means every open buffer appears in the bar,
      -- all living in a single vim tab — no unexpected tab creation.
      mode = "buffers",
    },
  },
}
